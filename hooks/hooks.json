{
  "description": "Collect local PAVE phase timing and token totals without storing prompts or transcript contents or controlling tool execution.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "for pave_root in \"${CLAUDE_PLUGIN_ROOT:-}\" \"${PLUGIN_ROOT:-}\"; do if [ -n \"$pave_root\" ] && [ -f \"$pave_root/scripts/usage.js\" ]; then exec node \"$pave_root/scripts/usage.js\" hook; fi; done; exit 0",
            "timeout": 5
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "^update_plan$",
        "hooks": [
          {
            "type": "command",
            "command": "for pave_root in \"${CLAUDE_PLUGIN_ROOT:-}\" \"${PLUGIN_ROOT:-}\"; do if [ -n \"$pave_root\" ] && [ -f \"$pave_root/scripts/usage.js\" ]; then exec node \"$pave_root/scripts/usage.js\" hook; fi; done; exit 0",
            "timeout": 5
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "for pave_root in \"${CLAUDE_PLUGIN_ROOT:-}\" \"${PLUGIN_ROOT:-}\"; do if [ -n \"$pave_root\" ] && [ -f \"$pave_root/scripts/usage.js\" ]; then exec node \"$pave_root/scripts/usage.js\" hook; fi; done; exit 0",
            "timeout": 5
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "for pave_root in \"${CLAUDE_PLUGIN_ROOT:-}\" \"${PLUGIN_ROOT:-}\"; do if [ -n \"$pave_root\" ] && [ -f \"$pave_root/scripts/usage.js\" ]; then exec node \"$pave_root/scripts/usage.js\" hook; fi; done; exit 0",
            "timeout": 3
          }
        ]
      }
    ]
  }
}
